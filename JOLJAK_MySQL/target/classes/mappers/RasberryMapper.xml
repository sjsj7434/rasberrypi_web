<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lsj.mapper.RasberryMapper">
	 	
	<select id="selectAll" resultType="com.lsj.domain.RasberryVO">
		select * from temp_hud ORDER BY time DESC
	</select>	
    
    
    <select id="login" resultType="com.lsj.domain.RasberryloginVO">
		SELECT serialnumber, passwd
		  FROM rasberry
		 WHERE serialnumber = #{serialnumber}
		 AND passwd = #{passwd}   
		 
	</select>
	
</mapper>  